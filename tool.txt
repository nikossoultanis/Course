#!/bin/bash

while IFS=$'/n' read -r line
do
	if [[ "$line" =~ \#.* ]]
	then
		echo "its a comment"
		
	else
		echo "not a comment"
			
				while IFS=$'/n' read -r line
				do
					echo $line | cut -f 1
				done < event.dat
					
done < event.dat
